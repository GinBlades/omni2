# Based on https://docs.docker.com/compose/rails/
version: "3.4"
services:
  db:
    image: postgres
    volumes:
      - psql:/var/lib/postgresql/data
  web:
    build:
      target: prod
      context: .
    volumes:
      - .:/myapp
    ports:
      - "3000:9292"
    depends_on:
      - db
  redis:
    image: redis
volumes:
  psql: ~
